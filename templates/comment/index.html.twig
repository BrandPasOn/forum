{% extends 'base.html.twig' %}

{% block title %}Hello CommentController!{% endblock %}

{% block body %}
    {{form_start(form)}}
    
        {{form_row(form.content, {'label' : 'Editez votre commentaire'})}}
        <button type="submit" class="bg-amber-500 hover:bg-amber-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">
            Editer</button>
    {{form_end(form)}}
{% endblock %}
